---
description: Generate or refresh an AgentScope report for the current Cursor workspace.
---

# AgentScope Scan

```bash
agentscope scan . --html --lang zh
```

Use a provided workspace path instead of `.` when the user supplies one. Add `--project-only` when the user wants only project-local agent settings.

Return the generated HTML path and key counts.
